<h1>My blog posts</h1>

<% @blog_posts.each do |blog_post| %>
  <% if blog_post.draft? %>
    <div class="p-1 rounded bg-gray-100 text-gray-900 mb-4">
      <h2 class="mt-0">
        <%= link_to blog_post.title, blog_post %>
        <span> - 📝</span>
      </h2>
      <p><%= blog_post.body%></p>
    </div>
  <% elsif blog_post.scheduled? %>
    <div class="p-2 rounded bg-blue-100 text-gray-900 mb-4">
      <h2 class="mt-0">
        <%= link_to blog_post.title, blog_post %>
        <span> - 🗓️</span>
      </h2>
      <p><%= blog_post.body%></p>
    </div>
  <% elsif blog_post.published? %>
    <% if user_signed_in? %>
      <div class="p-2 rounded bg-green-100 text-gray-900 mb-4">
        <h2 class="mt-0">
          <%= link_to blog_post.title, blog_post %>
          <span> - ✅</span>
        </h2>
        <p><%= blog_post.body%></p>
      </div>
    <% else %>
      <div class="border-2 border-black">
        <h2 class="mt-0">
          <%= link_to blog_post.title, blog_post %>
        </h2>
        <p><%= blog_post.body%></p>
      </div>
    <% end %>
  <% end %>
<% end%>
